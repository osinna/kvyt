[project]
name = "kvyt-gateway"
version = "0.1.0"
description = "KVYT gateway service"
requires-python = ">=3.12"
dependencies = [
    "kvyt_common==0.1.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
